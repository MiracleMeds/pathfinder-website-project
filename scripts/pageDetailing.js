import {strike, aid, crawl, delay} from "/scripts/mockDatabase.js";
//code used to highlight which action you are looking at. not in use yet.
// $.fn.isInViewport = function() {
//     var elementTop = $(this).offset().top;
//     var elementBottom = elementTop + $(this).outerHeight();
//     var viewportTop = $(window).scrollTop();
//     var viewportBottom = viewportTop + $(window).height();
//     return elementBottom > viewportTop && elementTop > viewportTop;
// };

//if "strike no longer in view, move to next one?"
// $(window).on('resize scroll', function() {
//     if ($('#Crawl').isInViewport()) {
//         console.log('#myElement is visible');
//     } else {
//         console.log('#myElement is not visible');
//     }
// });
